org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Must match Go server PORT (server/.env). 8080 on Windows is often PostgreSQL/EDB — use 8081 for the Go API.
JUKE_API_BASE=http://10.0.2.2:8081/
